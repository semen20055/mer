#include <stdio.h>

int main(void) {
    float a = 20.0f;
    float b = 4.0f;
    float c = 6.0f;
    float d = a + c;
    printf("a = %.1f, b = %.1f, c = %.1f, d = %.1f\n", a, b, c, d);
    return 0;
}